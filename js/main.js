// Main entry point for Daniel Mota's professional portfolio

document.addEventListener('DOMContentLoaded', () => {
  // Console signature for modern look
  console.log(
    '%cDaniel Mota | Portfólio Profissional',
    'color: #00d4ff; font-size: 16px; font-weight: bold; background: #0a0a0f; padding: 4px 8px; border-radius: 4px;'
  );
  
  // Future backend/CMS integration hook point
  initPortfolioData();
});

function initPortfolioData() {
  // This is ready to fetch dynamic configurations or projects in the future
  // e.g. fetch('/api/profile').then(res => res.json()).then(data => {...})
}
